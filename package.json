{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build --dotenv .env.dev",
    "build:qa": "nuxt build --dotenv .env.qa",
    "dev": "set PORT=5000 && nuxt dev --dotenv .env.dev",
    "generate": "nuxt generate --dotenv .env.dev",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare --dotenv .env.dev"
  },
  "dependencies": {
    "nuxt": "^3.14.1592",
    "rxjs": "^7.8.1",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "sass-embedded": "^1.83.0"
  }
}
